diff --git a/backend/agents/software_engineering_team/product_requirements_analysis_agent/question_processing.py b/backend/agents/software_engineering_team/product_requirements_analysis_agent/question_processing.py
index cad18d54..570c72d5 100644
--- a/backend/agents/software_engineering_team/product_requirements_analysis_agent/question_processing.py
+++ b/backend/agents/software_engineering_team/product_requirements_analysis_agent/question_processing.py
@@ -197,7 +197,7 @@ def parse_spec_review_response(raw: Any) -> SpecReviewResult:
         issues=issues,
         gaps=gaps,
         open_questions=open_questions,
-        summary=str(raw.get("summary") or "Spec review complete"),
+        summary=str(raw.get("summary", "") or "Spec review complete"),
     )
 
 
@@ -250,7 +250,7 @@ def parse_open_question(q_data: Any, index: int) -> OpenQuestion:
             id=str(q_data.get("id", f"q{index}")),
             question_text=str(q_data.get("question_text", "")),
             context=str(q_data.get("context", "")),
-            recommendation=str(q_data.get("recommendation", "")),
+            recommendation=str(q_data.get("recommendation", "") or ""),
             options=options,
             allow_multiple=bool(q_data.get("allow_multiple", False)),
             source=str(q_data.get("source", "spec_review")),
@@ -261,10 +261,10 @@ def parse_open_question(q_data: Any, index: int) -> OpenQuestion:
             depends_on=depends_on,
             blocking=bool(q_data.get("blocking", True)),
             owner=str(q_data.get("owner", "user")),
-            section_impact=list(q_data.get("section_impact", [])),
+            section_impact=list(q_data.get("section_impact", []) or []),
             due_date=str(q_data.get("due_date", "")),
             status=str(q_data.get("status", "open")),
-            asked_via=list(q_data.get("asked_via", [])),
+            asked_via=list(q_data.get("asked_via", []) or []),
         )
 
     return OpenQuestion(
@@ -588,7 +588,7 @@ def add_recommendations(
         if not isinstance(recs, list):
             return list(open_questions)
         rec_by_id = {
-            r.get("id"): str(r.get("recommendation", ""))
+            r.get("id"): str(r.get("recommendation", "") or "")
             for r in recs
             if isinstance(r, dict) and "id" in r
         }
